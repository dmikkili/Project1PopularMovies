<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.Project1PopularMovies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.android.Project1PopularMovies.PopularMovies"
            android:label="@string/appName">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.android.Project1PopularMovies.Details"
            android:label="@string/details">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.android.Project1PopularMovies.PopularMovies" />
        </activity>
        <activity
            android:name="com.example.android.Project1PopularMovies.SettingsActivity"
            android:label="@string/actionSettings">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.android.Project1PopularMovies.PopularMovies" />
        </activity>
    </application>

</manifest>
